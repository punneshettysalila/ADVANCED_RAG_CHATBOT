# A2A Multi-Agent Chatbot — Dependencies

# ── Core LLM & Agents ──────────────────────────────────────
huggingface_hub>=0.22.0          # HF InferenceClient + token auth

# ── Web Server (for FastAPI backend) ───────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0                # Optional production process manager on Render

# ── Streamlit UI ───────────────────────────────────────────
streamlit>=1.35.0
requests>=2.31.0                 # Streamlit → FastAPI HTTP client

# ── Utilities ──────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
python-multipart>=0.0.9
pypdf>=5.0.0

# Optional grounding accelerators
# faiss-cpu>=1.8.0
# chromadb>=0.5.0
# sentence-transformers>=3.0.0
